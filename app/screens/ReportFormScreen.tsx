import { View, Text } from 'react-native';

export default function ReportFormScreen() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Probleem Melden</Text>
        </View>
    );
}